---
title: "Reporte_ventas-JOYERÍA"
output: word_document
date: "2025-04-06"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r}

library(ggrepel)  # Para evitar superposición de etiquetas en gráficos
library(ggplot2)
library(scales)
library(janitor)
library(magick)

```
QUITO LOS ANTICIPOS POR ANILLOS DOS LETRAS PERO PONERLOS CON EL MONTO ENTERO CUANDO PAGUEN EL SALDO!!

#1.Corro Arreglo_base_Odoo
#2.Corro este

```{r filtro para el mes a analizar}

#Estas son las 3 cosas que tengo que hacer por San Telmo 2 cuando incluye período que abarca ventas tanto en joyería como cuero y que tenga stock de los 2
# QUITAR EL IS.NA(PRECIO) DEL STOCK QUE ES SOLO PARA ABRIL Y EL ANTI_JOIN DE STOCK SIN VENTA!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
# Y HABILITAR ESTO: #col_fact_es <- if (has_sufij) "San Telmo 2.y"  else "San Telmo 2"



fecha_inicio <- as.Date("2025/12/09") 
fecha_fin <- as.Date("2026/07/26")

dias_periodo <- as.integer(fecha_fin - fecha_inicio) + 1

fecha_stock <- strftime("2026/07/27","%Y%m%d")

fecha_precios <- strftime("2026/07/27","%Y%m%d")

fecha_proveedores <- strftime("2026/07/27","%Y%m%d")

```

```{r filtro las facturas por fecha y tiendas}

FACTURAS_MES_JOYERIA <- Odoo_mas_Alegra %>%
  select(-PRECIO_UNITARIO_SIN_IMP) %>% 
  filter(CODIGO_BARRAS != "Anticipo") |> 
  filter(as.Date(FECHA_EMISION) >= fecha_inicio,
         as.Date(FECHA_EMISION) <= fecha_fin,
         TIENDA %in% c("Güemes 2", "El Solar", "San Telmo 2")) %>% 
  filter(TIENDA != "San Telmo 2" | FECHA_EMISION > as.Date("2026-05-22")) %>% #filtro, para ST2, desde que es joyería   
  mutate(SUBFAMILIA = case_when(
  RUBRO == "MARRO CHICA" ~ "ACCESORIOS DE CUERO",
  TRUE ~ SUBFAMILIA)) #Le agrego la categoría a lo que es cuero dentro de lo que va a llamarse FAMILIA en el reporte 

REVISAR_SUBFAMILIA_JOYERIA <- FACTURAS_MES_JOYERIA %>% 
 # filter(PRODUCTO_NOMBRE_COMPLETO != "Anticipo del cliente") %>% 
  filter(is.na(SUBFAMILIA))

if (nrow(REVISAR_SUBFAMILIA_JOYERIA) > 0)
  message("⚠️  SUBFAMILIA sin asignar: ", nrow(REVISAR_SUBFAMILIA_JOYERIA), " fila(s)\n",
          paste(unique(REVISAR_SUBFAMILIA_JOYERIA$DESCRIPCION), collapse = ", "))

```




```{r elimino las facturas y/o notas de crédito que tienen sólo una diferencia mínima ante un cambio para que no lo compute como un artículo}

FACTURAS_MES2_JOYERIA <- FACTURAS_MES_JOYERIA %>% 
  filter(PRECIO_UNITARIO>100) 

```

```{r le agrego los proveedores}

proveedores_odoo_LUCERO_a_revisar <- read_excel(paste0("Proveedores/proveedores_odoo_LUCERO_", fecha_proveedores, ".xlsx")) |> 
  rename(CODIGO_BARRAS = "Código de barras", PRODUCTO = Producto, PROVEEDOR = Proveedores) %>% 
  mutate(PROVEEDOR = str_squish(PROVEEDOR)) %>% #para quitar espacios en blanco al ppio y al final del texto
  mutate(PROVEEDOR = if_else(
  PROVEEDOR %in% c("SUCCESSFUL BOXES S.R.L., PERDRIEL", "SUCCESSFUL BOXES S.R.L., CHACO", "SUCCESSFUL BOXES S.R.L., SD 2463"),
  "SUCCESSFUL BOXES S.R.L.",
  PROVEEDOR)) %>%
  mutate(PROVEEDOR = if_else(PROVEEDOR %in% c("PASSA DISEGNI SRL", "PASSA DISEGNI S.R.L"), "PASSA DISEGNI S.R.L. (ALAIMO)", PROVEEDOR)) |> 
  mutate(PROVEEDOR = if_else(is.na(PROVEEDOR) & PRODUCTO == "[102536] PULSERA PIEDRA NATURAL MEDIANA", "Muestra", PROVEEDOR))

#Reviso los proveedores entre aquellos productos con más de uno 
REVISO_PROVEEDORES <- proveedores_odoo_LUCERO_a_revisar |>
  filter(PRODUCTO != "[Delivery_007] Envío estándar" | is.na(PRODUCTO)) %>% #borro esta fila que aparece en primer lugar
  fill(CODIGO_BARRAS, PRODUCTO, .direction = "down") |>
  group_by(PRODUCTO) |>
  summarise(
    n_proveedores = n_distinct(PROVEEDOR),
    proveedores   = paste(sort(unique(PROVEEDOR)), collapse = " | "),
    .groups = "drop"
  ) %>% 
  filter(n_proveedores > 1)

#Elijo con qué proveedor quedarme entre los repetidos y completo faltantes
proveedores_odoo_LUCERO_revisado <- proveedores_odoo_LUCERO_a_revisar %>% 
  mutate(PROVEEDOR = if_else(PRODUCTO %in% c("[106037] PULSERA GRUMET PLANA 1.0 (16)", "[106040] PULSERA ROLO OVAL 0.6 (16)", "[106043] PULSERA ROLO OVAL 1.0 (16)"), "DE CUSATIS FLAVIO A Y DE CUSATIS VALERIOL C SOC DE HECHO", PROVEEDOR),
         PROVEEDOR = if_else(PRODUCTO %in% c("[106037] PULSERA GRUMET PLANA 1.0 (16)", "[106040] PULSERA ROLO OVAL 0.6 (16)", "	[106043] PULSERA ROLO OVAL 1.0 (16)"), "DE CUSATIS FLAVIO A Y DE CUSATIS VALERIOL C SOC DE HECHO", PROVEEDOR))
  

#Le cambio el nombre a los proveedores para que estén como los quiere Carlos
Proveedores_joyeria_Carlos <- read_excel("Proveedores/Proveedores_joyeria_Carlos.xlsx")

proveedores_odoo_LUCERO <- proveedores_odoo_LUCERO_revisado |> 
  left_join(Proveedores_joyeria_Carlos, by = "PROVEEDOR") |> 
  rename(PROVEEDOR_ORIGINAL = PROVEEDOR, PROVEEDOR = NOMBRE_NUEVO)


FACTURAS_MES2_JOYERIA$PROVEEDOR <- proveedores_odoo_LUCERO$PROVEEDOR[match(FACTURAS_MES2_JOYERIA$CODIGO_BARRAS, proveedores_odoo_LUCERO$CODIGO_BARRAS)]

FACTURAS_MES2_JOYERIA <- FACTURAS_MES2_JOYERIA |> 
  mutate(PROVEEDOR = case_when(DESCRIPCION %in% c("ANILLO DOS LETRAS", "ANILLO INICIAL (2)") ~ "MARIEL LINI",
                               TRUE ~PROVEEDOR))

REVISAR_PROVEEDORES_JOYERIA <- FACTURAS_MES2_JOYERIA %>% 
  #filter(PRODUCTO_NOMBRE_COMPLETO != "Anticipo del cliente") %>% 
  filter(is.na(PROVEEDOR))

if (nrow(REVISAR_PROVEEDORES_JOYERIA) > 0)
  message("⚠️  PROVEEDOR sin asignar: ", nrow(REVISAR_PROVEEDORES_JOYERIA), " fila(s)\n",
          paste(unique(REVISAR_PROVEEDORES_JOYERIA$DESCRIPCION), collapse = ", "))

```


#Creo cada una de las solapas
###1ro calculo el total de descuento y el dto por tienda
```{r}
DESCUENTO_JOYERIA <- FACTURAS_MES2_JOYERIA %>% 
  select(NRO_FACTURA, DESCUENTO) %>% 
  distinct()

DESCUENTO_X_TIENDA_JOYERIA <- FACTURAS_MES2_JOYERIA |> 
  select(TIENDA, NRO_FACTURA, DESCUENTO) %>% 
  distinct() |> 
  group_by(TIENDA) |> 
  summarise(DTO_X_TIENDA = sum(DESCUENTO, na.rm = TRUE)) |> 
  ungroup()

DESCUENTO_TOTAL_JOYERIA <- sum(DESCUENTO_JOYERIA$DESCUENTO, na.rm = TRUE)

```


##Concateno los atributos
```{r}

FACTURAS_MES2_JOYERIA_CONCA <- FACTURAS_MES2_JOYERIA %>% 
  mutate(PRODUCTO = pmap_chr(
    list(DESCRIPCION, COLOR, MATERIAL, TALLE, VARIANTE, MEDIDA, DIAMETRO, GROSOR, PESO, PIEDRA, TERMINACION),
    \(...) str_flatten(na.omit(c(...)), collapse = " - ")
  ))
  
```


## SKU
```{r solapa SKU}

SKU_JOYERIA <- FACTURAS_MES2_JOYERIA_CONCA %>% 
  group_by(PROVEEDOR, SUBFAMILIA, CODIGO_BARRAS, PRODUCTO, PRODUCTO_STOCK) %>% 
  summarise(VENTA = sum(PRODUCTO_CANTIDAD, na.rm = TRUE), 
            TOTAL = round(sum(PRODUCTO_TOTAL, na.rm = TRUE)), 
            PRECIO = round(TOTAL/VENTA, digits=0)) %>% 
  rename("CODIGO"=CODIGO_BARRAS) %>% 
  filter(VENTA != 0) |> 
  arrange(desc(TOTAL)) %>% 
  ungroup() |> 
  rename(STOCK = PRODUCTO_STOCK) |> 
  relocate(PRECIO, .after = VENTA) |> 
  relocate(STOCK, .after = TOTAL)

# Calculo S/D
SKU_SD_JOYERIA <- SKU_JOYERIA %>% 
  mutate("D/S" = if_else(VENTA>0, round(STOCK/VENTA* dias_periodo, digits = 0), NA_real_))


# Creo la fila con descuento
descuento_sku_JOYERIA <- tibble(
  PROVEEDOR = "DESCUENTO",
  SUBFAMILIA = NA,
  CODIGO = NA,
  PRODUCTO = NA,
  VENTA = NA,
  PRECIO = NA,
  TOTAL = DESCUENTO_TOTAL_JOYERIA,
  STOCK = NA,
  `D/S` = NA)

# Agrego la fila con valor de descuento
#SKU_desc_JOYERIA <- bind_rows(descuento_sku_JOYERIA, SKU_SD_JOYERIA)


# Creo la fila con los totales
totales_sku_JOYERIA <- tibble(
  PROVEEDOR = "TOTAL",
  SUBFAMILIA = NA,
  CODIGO = NA,
  PRODUCTO = NA,
  VENTA = sum(SKU_SD_JOYERIA$VENTA, na.rm = TRUE),
  PRECIO = NA,
  TOTAL = sum(SKU_SD_JOYERIA$TOTAL, na.rm = TRUE)-DESCUENTO_TOTAL_JOYERIA,
  STOCK = sum(SKU_SD_JOYERIA$STOCK, na.rm = TRUE),
  `D/S` = NA)

# Agregar la fila al inicio de los datos
SKU_total_JOYERIA <- bind_rows(totales_sku_JOYERIA, SKU_SD_JOYERIA) %>% 
  rename(FAMILIA = SUBFAMILIA)

```


##SKU-FAMILIA
```{r}

SKU_SUBFAMILIA_JOYERIA <- SKU_SD_JOYERIA %>%
  select(PROVEEDOR, SUBFAMILIA, CODIGO, PRODUCTO, VENTA, PRECIO, TOTAL, STOCK, `D/S`)

# 1. Ordenar los datos originales
SKU_SUBFAMILIA_JOYERIA_ordenado <- SKU_SUBFAMILIA_JOYERIA %>%
  arrange(SUBFAMILIA, PRODUCTO, desc(TOTAL))

# 2. Crear los totales por familia
totales_por_familia_JOYERIA <- SKU_SUBFAMILIA_JOYERIA %>%
  group_by(SUBFAMILIA) %>%
  summarise(
    PROVEEDOR = "",
    CODIGO = "",
    PRODUCTO = paste("Total", first(SUBFAMILIA)),
    PRECIO = NA_real_,
    `D/S` = NA_real_,
    VENTA = sum(VENTA, na.rm = TRUE),
    TOTAL = round(sum(TOTAL, na.rm = TRUE)),
    STOCK = sum(STOCK, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  distinct(SUBFAMILIA, PRODUCTO, .keep_all = TRUE) %>% # Asegura un único total
  select(names(SKU_SUBFAMILIA_JOYERIA))

# 3. Combinar y ordenar
SKU_SUBFAMILIA_JOYERIA_final <- bind_rows(
  SKU_SUBFAMILIA_JOYERIA_ordenado,
  totales_por_familia_JOYERIA
) %>%
  mutate(
    tipo_fila = ifelse(str_detect(PRODUCTO, "^Total"), 1, 0)
  ) %>%
  arrange(
    SUBFAMILIA,
    tipo_fila,  # 0=productos primero, 1=totales al final
    desc(TOTAL)
  ) %>%
  select(-tipo_fila) %>% 
  mutate(SUBFAMILIA = ifelse(str_detect(PRODUCTO, "^Total"), "", SUBFAMILIA)) %>% 
  rename(FAMILIA = SUBFAMILIA)

# SKU_SUBFAMILIA_JOYERIA_final <- SKU_SUBFAMILIA_JOYERIA_final %>% 
#   mutate(CONDICION = ifelse(RUBRO == "OUTLET", "outlet", CONDICION))
  
```

## Modelos
```{r solapa Modelos}

#los modelos pueden tener distinto precio por color o por inflación, entonces se muestra el precio unitario calculado Total/venta

Descripcion <- FACTURAS_MES2_JOYERIA_CONCA %>% 
  select(DESCRIPCION, PRODUCTO) %>% 
  distinct()

Modelos_JOYERIA <- SKU_total_JOYERIA %>% 
  filter(PROVEEDOR != "TOTAL" & PROVEEDOR != "DESCUENTO") %>%
  left_join(Descripcion, by = "PRODUCTO") %>% 
  select(-PRODUCTO) %>% 
  group_by(DESCRIPCION) |> 
  mutate(CODIGO = min(CODIGO)) |> #para quedarme sólo con un ´codigo por descripción. Si lo ponía después, no funcionaba bien porque mantenía uno por tienda
  group_by(PROVEEDOR, FAMILIA, DESCRIPCION, CODIGO) %>% 
  summarise(VENTA = sum(VENTA, na.rm = TRUE), 
            TOTAL = sum(TOTAL, na.rm = TRUE), 
            PRECIO = round(TOTAL/VENTA), 
            STOCK = sum(STOCK, na.rm = TRUE), 
            "D/S" = round(STOCK/VENTA* dias_periodo)) %>%
  distinct() %>% 
  arrange(desc(TOTAL)) %>% 
  ungroup() %>% 
  relocate(PRECIO, .after = VENTA)

# Creo la fila con el descuento
desc_modelos_JOYERIA <- tibble(
  PROVEEDOR = "DESCUENTO",
  FAMILIA = NA,
  DESCRIPCION = NA,
  CODIGO = NA,
  PRECIO = NA,
  VENTA = NA,
  TOTAL = DESCUENTO_TOTAL_JOYERIA,
  STOCK = NA,
  `D/S` = NA)

# Agregar la fila al inicio del marco de datos
#Modelos_desc_JOYERIA <- bind_rows(desc_modelos_JOYERIA, Modelos_JOYERIA)

# Creo la fila con los totales
totales_modelos_JOYERIA <- tibble(
  PROVEEDOR = "TOTAL",
  FAMILIA = NA,
  DESCRIPCION = NA,
  CODIGO = NA,
  PRECIO = NA,
  VENTA = sum(Modelos_JOYERIA$VENTA, na.rm = TRUE),
  TOTAL = sum(Modelos_JOYERIA$TOTAL, na.rm = TRUE)-DESCUENTO_TOTAL_JOYERIA,
  STOCK = sum(Modelos_JOYERIA$STOCK, na.rm = TRUE),
  `D/S` = NA)

# Agregar la fila al inicio del marco de datos
Modelos_total_JOYERIA <- bind_rows(totales_modelos_JOYERIA, Modelos_JOYERIA)

# Ordeno columnas
Modelos_total_JOYERIA <- Modelos_total_JOYERIA %>% 
  relocate(PRECIO, .after = VENTA)
```


##FAMILIAS
```{r hoja subfamilias}

#QUEDA EN BLANCO LA FILA DE LOS ANTICIPOS. VER CÓMO MANEJARLOS CUANDO EL TOTAL SE MAYOR A 0

#TABLA SUBFAMILIAS
tabla_vta_subfamilia_JOYERIA <- FACTURAS_MES2_JOYERIA_CONCA %>%
  group_by(SUBFAMILIA) %>%
  summarise(Unidades = sum(PRODUCTO_CANTIDAD, na.rm = TRUE), 
            "Facturación" = sum(PRODUCTO_TOTAL, na.rm = TRUE)) %>%
  complete(SUBFAMILIA = unique(FACTURAS_MES2_JOYERIA$SUBFAMILIA),  # Lista completa de rubros
           fill = list(Unidades = 0, Facturación = 0)) %>%
  mutate(Facturación = if_else(SUBFAMILIA == "DIJES Y COLGANTES", `Facturación` - 2551, Facturación)) |>  #por diferencia a favor nuestra en devolución de "DIJE INICIAL - PLATA 925 - N"
  arrange(desc(Facturación)) %>% 
  mutate(Participación = round(Facturación/sum(Facturación), digits = 4))

totales_venta_subfamilia_JOYERIA <- tabla_vta_subfamilia_JOYERIA %>%
  summarise(SUBFAMILIA = "TOTAL",
            Unidades = sum(Unidades, na.rm = TRUE),
            Facturación = sum(Facturación, na.rm = TRUE)-DESCUENTO_TOTAL_JOYERIA,
            Participación = 1)


fila_descuento_subfamilia_JOYERIA <- tibble(
  SUBFAMILIA = "DESCUENTO",
  Unidades = NA,
  Facturación = DESCUENTO_TOTAL_JOYERIA,
  Participación = NA)

#tabla_venta_subfamilia_JOYERIA <- bind_rows(fila_descuento_subfamilia_JOYERIA, tabla_vta_subfamilia_JOYERIA)

tabla_venta_subfamilia_JOYERIA <- bind_rows(totales_venta_subfamilia_JOYERIA, tabla_vta_subfamilia_JOYERIA) %>% 
  filter(!is.na(SUBFAMILIA)) %>% 
  rename(FAMILIA = SUBFAMILIA)

```



##Atributos
###Talles
```{r atributo: talles}

#CADENAS
CADENAS_TALLE <- FACTURAS_MES2_JOYERIA %>%
  filter(SUBFAMILIA == "CADENAS Y COLLARES") %>% 
  filter(str_detect(DESCRIPCION, regex("CADENA", ignore_case = TRUE))) %>%
  filter(!is.na(TALLE)) %>% 
  group_by(TALLE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(TALLE)


#PULSERAS
PULSERAS_TALLE <- FACTURAS_MES2_JOYERIA %>%
  filter(SUBFAMILIA == "PULSERAS") %>% 
  filter(!is.na(TALLE)) %>% 
  group_by(TALLE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(TALLE)


#ANILLOS
ANILLOS_TALLE <- FACTURAS_MES2_JOYERIA %>%
  filter(SUBFAMILIA == "ANILLOS") %>% 
  mutate(TALLE = if_else(CODIGO_BARRAS == "106269", "16", TALLE)) |> 
  group_by(TALLE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>%
  filter(Facturación>0) %>%
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(TALLE) 


```

###Grosor
```{r atributo: Grosor}

#CADENAS
CADENAS_GROSOR <- FACTURAS_MES2_JOYERIA %>%
  filter(SUBFAMILIA == "CADENAS Y COLLARES") %>% 
  filter(str_detect(DESCRIPCION, regex("CADENA", ignore_case = TRUE))) %>%
  filter(!is.na(GROSOR)) %>% 
  group_by(GROSOR) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(GROSOR)


#PULSERA
PULSERAS_GROSOR <- FACTURAS_MES2_JOYERIA %>%
  filter(SUBFAMILIA == "PULSERAS") %>% 
  filter(!is.na(GROSOR)) %>% 
  group_by(GROSOR) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(GROSOR)


```


###Variante de anillos
```{r Atributo: Variante de anillos}

#ANILLOS
ANILLOS_ZODIACO_VARIANTE <- FACTURAS_MES2_JOYERIA %>%
  filter(DESCRIPCION == "ANILLO ZODIACO") %>% 
  group_by(VARIANTE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>%
  filter(Facturación >0) %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Facturación))

ANILLOS_DOS_LETRAS_VARIANTE <- FACTURAS_MES2_JOYERIA %>%
  filter(DESCRIPCION == "ANILLO DOS LETRAS") %>%
  mutate(VARIANTE = if_else(CODIGO_BARRAS == "106269", "MA", VARIANTE)) |> 
  group_by(VARIANTE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Facturación))

ANILLOS_INICIAL_VARIANTE <- FACTURAS_MES2_JOYERIA %>%
  filter(str_detect(DESCRIPCION, "ANILLO INICIAL")) %>% 
  group_by(VARIANTE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>%
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Facturación))

```


###Variantes de Dijes
```{r atributo de dijes}

#VARIANTE
DIJE_INICIAL_VARIANTE <- FACTURAS_MES2_JOYERIA %>%
  filter(str_detect(DESCRIPCION, regex("DIJE INICIAL", ignore_case = TRUE))) %>% 
  group_by(VARIANTE) %>% 
  summarise(Facturación = sum(PRODUCTO_TOTAL, na.rm = TRUE),
            Cantidad = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
                              .groups = 'drop') %>% 
  filter(Cantidad>0) |> 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Facturación))

```



#Proveedores
```{r}

#Tabla general proveedor
TABLA_PROVEEDOR <- FACTURAS_MES2_JOYERIA_CONCA %>% 
 # filter(!is.na(PROVEEDOR)) %>%
  group_by(PROVEEDOR) %>% 
  summarise(Unidades = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
    Facturación = round(sum(PRODUCTO_TOTAL, na.rm = TRUE))) %>% 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Facturación))

fila_total_proveedor_JOYERIA <- tibble(
  PROVEEDOR = "TOTAL",
  Unidades = sum(TABLA_PROVEEDOR$Unidades, na.rm = TRUE),
  Facturación = sum(TABLA_PROVEEDOR$Facturación, na.rm = TRUE) - DESCUENTO_TOTAL_JOYERIA - 2551, #por diferencia a favor nuestra en devolución de "DIJE INICIAL - PLATA 925 - N"
  Participación = 1)

PROVEEDOR_JOYERIA_TOTAL <- bind_rows(fila_total_proveedor_JOYERIA, TABLA_PROVEEDOR) 
  
  
#Tabla de familias entre los proveedores  
PROVEEDOR_SUBFAMILIAS <- FACTURAS_MES2_JOYERIA %>% 
  select(-FAMILIA) %>% 
  rename(FAMILIA = SUBFAMILIA) %>% 
  group_by(FAMILIA, PROVEEDOR) %>% 
  summarise(Unidades = sum(PRODUCTO_CANTIDAD, na.rm = TRUE),
    Facturación = round(sum(PRODUCTO_TOTAL, na.rm = TRUE))) %>% 
  mutate(Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 3)) %>% 
  filter(Unidades>0) %>% 
  arrange(FAMILIA, desc(Facturación))
  
fila_total_proveedor_subfamilia_JOYERIA <- tibble(
  FAMILIA = "TOTAL",
  Unidades = sum(PROVEEDOR_SUBFAMILIAS$Unidades, na.rm = TRUE),
  Facturación = sum(PROVEEDOR_SUBFAMILIAS$Facturación, na.rm = TRUE)- DESCUENTO_TOTAL_JOYERIA - 2551, #por diferencia a favor nuestra en devolución de "DIJE INICIAL - PLATA 925 - N,
  Participación = 1)

# Subtotales por familia
subt_fam_prov <- PROVEEDOR_SUBFAMILIAS %>%
  group_by(FAMILIA) %>%
  summarise(
    PROVEEDOR     = paste("Total", first(FAMILIA)),
    Unidades      = sum(Unidades, na.rm = TRUE),
    Facturación   = sum(Facturación, na.rm = TRUE),
    Participación = sum(Participación, na.rm = TRUE),
    .groups = "drop"
  )

# Combinar: subtotales después de los datos de cada familia
PROVEEDOR_SUBFAMILIAS_con_subt <- bind_rows(
  PROVEEDOR_SUBFAMILIAS %>% mutate(tipo_fila = 0),
  subt_fam_prov %>% mutate(tipo_fila = 1)
) %>%
  arrange(FAMILIA, tipo_fila, desc(Facturación)) %>%
  select(-tipo_fila)

PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA <- bind_rows(
  fila_total_proveedor_subfamilia_JOYERIA,
  PROVEEDOR_SUBFAMILIAS_con_subt
)

```


#Método de pago
```{r}

# Tabla con datos de Odoo
Metodo_pago_Odoo_resumido <- Metodo_pago_final %>%
   mutate(#AÑO = year(Fecha),
  #        MES = month(Fecha),
         Fecha = (as.Date(Fecha)),
         CONCEPTO = case_when(METODO_PAGO == "Pago electrónico Crédito" ~ "Tarjeta crédito",
                              METODO_PAGO == "Pago electrónico Débito" ~ "Tarjeta débito",
                              TRUE ~ METODO_PAGO)) %>%
  filter(TIENDA %in% c("Güemes 2", "El Solar", "San Telmo 2")) %>%
  filter(Fecha>=fecha_inicio & Fecha <= fecha_fin) %>%
  filter(TIENDA != "San Telmo 2" | Fecha > as.Date("2026-05-22")) %>% #filtro, para ST2, desde que es joyería
    group_by(TIENDA, CONCEPTO) %>%
  summarise(FACTURACION = sum(TOTAL_FACTURA),
            VENTA = n(),
            .groups = "drop") %>%
  group_by(TIENDA) %>%
  mutate(PARTICIPACION = FACTURACION / sum(FACTURACION))

```


#Nacionalidad
```{r}
#Calculo las unidades vendidas por nacionalidad y tienda
NACIONALIDAD_X_UNIDAD_JOYERIA <- FACTURAS_MES2_JOYERIA %>%
  group_by(NACIONALIDAD, TIENDA) %>%
  summarise(n = sum(PRODUCTO_CANTIDAD, na.rm = TRUE), .groups = "drop") %>%
  pivot_wider(names_from = TIENDA, values_from = n, values_fill = 0) %>%
  mutate(`Total cantidad` = rowSums(select(., -NACIONALIDAD), na.rm = TRUE))

fila_total_nacionalidad_unidad_JOYERIA <- NACIONALIDAD_X_UNIDAD_JOYERIA %>%
  summarise(across(-NACIONALIDAD, ~ sum(.x, na.rm = TRUE))) %>%
  mutate(NACIONALIDAD = "Total general") %>%
  relocate(NACIONALIDAD)
NACIONALIDAD_X_UNIDAD_TOTAL_JOYERIA <- bind_rows(fila_total_nacionalidad_unidad_JOYERIA, NACIONALIDAD_X_UNIDAD_JOYERIA)


#Calculo la facturación por nacionalidad y tienda
NACIONALIDAD_X_FACTURACION_JOYERIA <- FACTURAS_MES2_JOYERIA %>%
  # 1. Nos aseguramos de tener 1 fila por factura
 # distinct(TIENDA, NRO_FACTURA, .keep_all = TRUE) %>%
  # 2. Sumamos por Nacionalidad y Tienda
  group_by(NACIONALIDAD, TIENDA) %>%
  summarise(TOTAL = sum(PRODUCTO_TOTAL, na.rm = TRUE), .groups = "drop") %>%
  # 3. Pivoteamos para que cada tienda sea una columna
  pivot_wider(names_from = TIENDA, values_from = TOTAL, values_fill = 0) %>%
  # 4. Sumamos la fila completa para el Total
  mutate(`Total facturación` = rowSums(select(., -NACIONALIDAD), na.rm = TRUE))

fila_total_nacionalidad_facturacion_JOYERIA <- NACIONALIDAD_X_FACTURACION_JOYERIA %>%
  summarise(across(-NACIONALIDAD, ~ sum(.x, na.rm = TRUE))) %>%
  mutate(NACIONALIDAD = "Total general") %>%
  relocate(NACIONALIDAD)

# Restar descuentos por tienda solo para las que existen
for (t in DESCUENTO_X_TIENDA_JOYERIA$TIENDA) {
  if (t %in% names(fila_total_nacionalidad_facturacion_JOYERIA)) {
    dto <- DESCUENTO_X_TIENDA_JOYERIA$DTO_X_TIENDA[DESCUENTO_X_TIENDA_JOYERIA$TIENDA == t]
    fila_total_nacionalidad_facturacion_JOYERIA[[t]] <-
      fila_total_nacionalidad_facturacion_JOYERIA[[t]] - dto
  }
}

fila_total_nacionalidad_facturacion_JOYERIA <- fila_total_nacionalidad_facturacion_JOYERIA %>%
  mutate(`Total facturación` = sum(NACIONALIDAD_X_FACTURACION_JOYERIA$`Total facturación`, na.rm = TRUE) -
           sum(DESCUENTO_X_TIENDA_JOYERIA$DTO_X_TIENDA)-2550) #2550 crédito a favor de la tienda

NACIONALIDAD_X_FACTURACION_TOTAL_JOYERIA <- bind_rows(fila_total_nacionalidad_facturacion_JOYERIA, NACIONALIDAD_X_FACTURACION_JOYERIA)

#Uno las dos tablas (cantidad y facturación)
NACIONALIDAD_ENTERA_JOYERIA <- NACIONALIDAD_X_UNIDAD_TOTAL_JOYERIA %>% 
  left_join(NACIONALIDAD_X_FACTURACION_TOTAL_JOYERIA, by = "NACIONALIDAD") %>% 
  arrange(desc(`Total facturación`))

```


```{r venta por tiendas}

venta_x_tienda_JOYERIA <- FACTURAS_MES2_JOYERIA |> 
  select(FECHA_EMISION, NRO_FACTURA, TIENDA, TOTAL_FACTURA) |> 
  distinct() |> 
  group_by(TIENDA) |> 
  summarise(TOTAL = sum(TOTAL_FACTURA, na.rm = TRUE)) |> 
  ungroup()


unidades_x_tienda_JOYERIA <- FACTURAS_MES2_JOYERIA |> 
  select(FECHA_EMISION, NRO_FACTURA, CODIGO_BARRAS,  TIENDA, PRODUCTO_CANTIDAD) |> 
  distinct() |> 
  group_by(TIENDA) |> 
  summarise(CANTIDAD = sum(PRODUCTO_CANTIDAD, na.rm = TRUE)) |> 
  ungroup()


```


#PRECIO UNITARIO
```{r}
#EN GENERAL
PRECIO_UNITARIO_JOYERIA <- FACTURAS_MES2_JOYERIA %>% 
  select(Precio = PRECIO_UNITARIO, CANTIDAD = PRODUCTO_CANTIDAD) %>% 
  mutate(Precio = round(Precio)) %>% 
  filter(CANTIDAD > 0) |> 
  group_by(Precio) %>% 
  summarise(Cantidad = sum(CANTIDAD, na.rm = TRUE), .groups = "drop") %>% 
  mutate(Facturación = Cantidad*Precio, Participación = round((Facturación / sum(Facturación, na.rm = TRUE)), 4)) %>% 
  arrange(desc(Participación)) %>% 
    mutate(Acumulado = cumsum(Participación)) %>%
  filter(Acumulado <= 0.81)

#POR FAMILIA
PRECIO_UNITARIO_SUBFAMILIA_datos <- FACTURAS_MES2_JOYERIA %>%
  select(FAMILIA = SUBFAMILIA, Precio = PRECIO_UNITARIO, CANTIDAD = PRODUCTO_CANTIDAD) %>%
  filter(CANTIDAD > 0) |> 
  mutate(Precio = round(Precio)) %>%
  group_by(FAMILIA, Precio) %>%
  summarise(Cantidad = sum(CANTIDAD, na.rm = TRUE), .groups = "drop") %>%
  mutate(Facturación = Cantidad * Precio) %>%
  group_by(FAMILIA) %>%
  mutate(
    Participación = round(Facturación / sum(Facturación, na.rm = TRUE), 4)
  ) %>%
  ungroup() %>%
  arrange(FAMILIA, desc(Precio)) %>%        # primero el orden final
  group_by(FAMILIA) %>%
  mutate(Acumulado = cumsum(Participación)) %>%   # acumulado en ese mismo orden
  ungroup()

# Subtotales por familia
subt_pu_fam <- PRECIO_UNITARIO_SUBFAMILIA_datos %>%
  group_by(FAMILIA) %>%
  summarise(
    Cantidad      = sum(Cantidad, na.rm = TRUE),
    Facturación   = sum(Facturación, na.rm = TRUE),
    Participación = 1,
    Acumulado     = 1,
    .groups       = "drop"
  ) %>%
  mutate(fam_orig = FAMILIA,
         FAMILIA  = paste("Total", FAMILIA),
         Precio   = NA_real_)

PRECIO_UNITARIO_SUBFAMILIA_JOYERIA <- bind_rows(
  PRECIO_UNITARIO_SUBFAMILIA_datos %>% mutate(tipo = 0, fam_orig = FAMILIA),
  subt_pu_fam %>% mutate(tipo = 1)
) %>%
  arrange(fam_orig, tipo, desc(Precio)) %>%
  select(-tipo, -fam_orig)

```

#STOCK
```{r cargo el stock de odoo}

Stock <- read_excel(paste0("Stock/Stock_", fecha_stock,".xlsx")) %>% 
  select(TIENDA = Ubicación, REFERENCIA_INTERNA = "Código de referencia de producto", PRODUCTO= Producto, CANTIDAD = "Cantidad en inventario") |> filter(TIENDA != "WHLUC/Fallado") |> 
  mutate(TIENDA = case_when(TIENDA == "WHASG/Stock" ~ "AS Güemes",
                            TIENDA == "WHASS/Stock" ~ "AS San Telmo",
                            TIENDA == "WHBAS/Barracas" ~ "AS Barracas", 
                            TIENDA == "WHBAS/Reserva" ~ "AS Reserva",
                            TIENDA == "WHBLU/Stock" ~ "Barracas", 
                            TIENDA == "WHFLO/Stock" ~ "Florida",
                            TIENDA == "WHFRE/Stock" ~ "French",
                            TIENDA == "WHGU1/Stock" ~ "Güemes",
                            TIENDA == "WHGU2/Stock" ~ "Güemes 2",
                            TIENDA == "WHLUC/Stock" ~ "Florida Depósito",
                            TIENDA == "WHREC/Stock" ~ "Recoleta", 
                            TIENDA == "WHSOL/Stock" ~ "El Solar",
                            TIENDA == "WHST1/Stock" ~ "San Telmo 1",
                            TIENDA == "WHST2/Stock" ~ "San Telmo 2",
                            TRUE ~ TIENDA))
  
#Selecciono las tiendas y el depósito y cambio a 0 las cantidades en negativo
Stock_JOYERIA <- Stock |> 
  filter(TIENDA %in% c("Güemes 2", "El Solar", "San Telmo 2", "Florida Depósito", "Barracas"),
         !is.na(REFERENCIA_INTERNA)) |> 
  mutate(CANTIDAD = if_else(CANTIDAD<0, 0, CANTIDAD))

#Le agrego las columnas que necesito de los productos
##Primero preparo los datos
Base_para_stock_JOYERIA <- Base_Articulos_Odoo_para_machear |> 
  select(CODIGO_BARRAS, REFERENCIA_INTERNA, RUBRO, FAMILIA = SUBFAMILIA, MATERIAL, COLOR, TALLE, VARIANTE, MEDIDA, DIAMETRO, GROSOR, PESO)

#FIltro bien para quedarme sólo con productos de joyería
Stock_con_subfamilia_JOYERIA <- Stock_JOYERIA |> 
  left_join(Base_para_stock_JOYERIA, by = "REFERENCIA_INTERNA") |> 
  filter(FAMILIA %in% c("ANILLOS", "AROS", "CADENAS Y COLLARES", "PULSERAS", "DIJES Y COLGANTES", "ACCESORIOS DE CUERO") |  str_detect(PRODUCTO, "TIENTO CUERO|COLLAR TUBO CUERO|ANILLO|PULSERA|BRAZALETE")) %>% 
  mutate(FAMILIA = case_when(
  RUBRO == "MARRO CHICA" ~ "ACCESORIOS DE CUERO",
  TRUE ~ FAMILIA)) #Le agrego la categoría a lo que es cuero dentro de lo que va a llamarse FAMILIA en el reporte 

#Le agrego los precios desde la lista de precios de Odoo
  Precios <- read_excel(paste0("Lista_precios/Lista_precios_", fecha_precios, ".xlsx")) |> 
    rename(PRODUCTO = "Reglas de lista de precios/Variante", PRECIO_SIN_IMP = "Reglas de lista de precios/Precio fijo") |> 
    mutate(PRECIO = round(PRECIO_SIN_IMP*1.21)) |>
    mutate(REFERENCIA_INTERNA = str_extract(PRODUCTO, "(?<=\\[)\\d{6}(?=\\])")) %>% 
    select(-PRECIO_SIN_IMP, -PRODUCTO) |> 
    distinct()
  
REVISAR_MAS_DE_UN_PRECIO <- Precios %>% 
  group_by(REFERENCIA_INTERNA) %>% 
  filter(n()>1)
  
  
Stock_con_precios_JOYERIA <- Stock_con_subfamilia_JOYERIA |> 
  left_join(Precios, by = "REFERENCIA_INTERNA") 


REVISAR_STOCK_SIN_PRECIO_JOYERIA <- Stock_con_precios_JOYERIA %>% 
filter(is.na(PRECIO)) #para eliminar las filas de productos que no están a la venta 

if (nrow(REVISAR_STOCK_SIN_PRECIO_JOYERIA) > 0)
  message("⚠️  STOCK sin precio: ", nrow(REVISAR_STOCK_SIN_PRECIO_JOYERIA), " fila(s)\n",
          paste(unique(REVISAR_STOCK_SIN_PRECIO_JOYERIA$PRODUCTO), collapse = ", "))

STOCK_TIENDA_JOYERIA <-  Stock_con_precios_JOYERIA %>%
 # filter(!is.na(PRECIO)) %>% #PARA BORRAR!!!!

  mutate(TOTAL = PRECIO*CANTIDAD) |> 
  select(FAMILIA, TIENDA, TOTAL) %>% 
  group_by(FAMILIA, TIENDA) %>% 
  summarise(VALOR_STOCK = sum(TOTAL, na.rm = TRUE)) |> 
  filter(VALOR_STOCK>0)


#calculo el valor del stock por tienda y depósito
STOCK_VALOR_JOYERIA <- STOCK_TIENDA_JOYERIA %>% 
  pivot_wider(
    id_cols = FAMILIA,    # Columna que irá a las filas
    names_from = TIENDA,       # Columna que irá a las columnas
    values_from = VALOR_STOCK    # Columna con los valores
  )

#agrego totales de columnas y las ordeno
STOCK_VALOR_TOTAL_JOYERIA <- STOCK_VALOR_JOYERIA %>% 
  adorn_totals(where = "row") %>% 
  relocate("Florida Depósito", .after = "San Telmo 2")
  
```


#Helpers
```{r helpers}

titulo_reporte <- paste0("REPORTE DE VENTA JOYERÍA ACUMULADO – al ", toupper(format(fecha_fin, "%d de %B de %Y")))

compress_img <- function(filepath, max_px = 180, quality = 72) {
  img  <- magick::image_read(filepath)
  info <- magick::image_info(img)
  if (info$width > max_px || info$height > max_px) {
    img <- magick::image_scale(img, paste0(max_px, "x", max_px))
  }
  tmp <- tempfile(fileext = paste0(".", tolower(tools::file_ext(filepath))))
  magick::image_write(img, path = tmp, quality = quality)
  tmp
}
```

```{r paleta de colores}

COL_HEADER <- "#2F4F6E"
COL_TOTAL  <- "#D9E1EA"
COL_DESCTO <- "#FFF3CD"

```

#Estilos
```{r estilos base}

st_header <- createStyle(
  fontName = "Arial", fontSize = 10, fontColour = "white",
  fgFill = COL_HEADER, halign = "CENTER", valign = "CENTER",
  textDecoration = "BOLD", border = "TopBottomLeftRight",
  borderColour = "#FFFFFF", wrapText = FALSE
)
st_total <- createStyle(
  fontName = "Arial", fontSize = 10,
  fgFill = COL_TOTAL, textDecoration = "BOLD",
  border = "TopBottomLeftRight", borderColour = "#B0BEC5"
)
st_descto <- createStyle(
  fontName = "Arial", fontSize = 10,
  fgFill = COL_DESCTO, textDecoration = "ITALIC",
  border = "TopBottomLeftRight", borderColour = "#E0C060"
)
st_normal <- createStyle(
  fontName = "Arial", fontSize = 10,
  border = "TopBottomLeftRight", borderColour = "#DEE2E6"
)
st_number <- createStyle(numFmt = "#,##0")
st_pct    <- createStyle(numFmt = "0.0%")
st_titulo_sec <- createStyle(
  fontName = "Arial", fontSize = 13,
  textDecoration = "BOLD", fontColour = COL_HEADER
)
st_titulo_bloque <- createStyle(
  fontName = "Arial", fontSize = 11,
  textDecoration = "BOLD", fontColour = COL_HEADER
)

```

```{r aplico estilos a tablas}

# start_row: fila del encabezado (1-indexed Excel)
# pct_cols / num_cols: índices relativos a la tabla (1 = primera columna del df)
apply_table_styles <- function(wb, sheet, df, start_row, start_col = 1,
                               total_rows  = NULL,
                               descto_rows = NULL,
                               pct_cols    = NULL,
                               num_cols    = NULL) {
  n_rows <- nrow(df)
  n_cols <- ncol(df)
  cols   <- start_col:(start_col + n_cols - 1)
  data_rows <- (start_row + 1):(start_row + n_rows)

  # Encabezado
  addStyle(wb, sheet, st_header, rows = start_row, cols = cols,
           gridExpand = TRUE)
  # Cuerpo normal
  addStyle(wb, sheet, st_normal, rows = data_rows, cols = cols,
           gridExpand = TRUE, stack = TRUE)
  # Filas TOTAL
  if (!is.null(total_rows))
    for (r in total_rows)
      addStyle(wb, sheet, st_total, rows = r, cols = cols,
               gridExpand = TRUE, stack = FALSE)
  # Filas DESCUENTO
  if (!is.null(descto_rows))
    for (r in descto_rows)
      addStyle(wb, sheet, st_descto, rows = r, cols = cols,
               gridExpand = TRUE, stack = FALSE)
  # Porcentajes
  if (!is.null(pct_cols))
    addStyle(wb, sheet, st_pct,
             rows = data_rows, cols = start_col + pct_cols - 1,
             gridExpand = TRUE, stack = TRUE)
  # Números con separador de miles
  if (!is.null(num_cols))
    addStyle(wb, sheet, st_number,
             rows = data_rows, cols = start_col + num_cols - 1,
             gridExpand = TRUE, stack = TRUE)
}

```

#Ggplot
```{r Guardar ggplot a PNG temporal}

save_plot_tmp <- function(p, width = 6, height = 4, dpi = 150) {
  tmp <- tempfile(fileext = ".png")
  ggsave(tmp, plot = p, width = width, height = height,
         dpi = dpi, bg = "white")
  tmp
}

```

```{r Tema ggplot compartido}

tema_reporte <- function() {
  theme_minimal(base_size = 10) +
    theme(
      plot.title    = element_text(colour = COL_HEADER, face = "bold", size = 11),
      axis.title    = element_blank(),
      panel.grid.major.y = element_blank(),
      legend.position    = "right"
    )
}

```


# WORKBOOK
```{r}

wb <- createWorkbook()

```

## 1. FAMILIAS RESUMEN

```{r}
addWorksheet(wb, "Resumen familias", gridLines = FALSE)
ws <- "Resumen familias"
```

### Título
```{r}
writeData(wb, ws, titulo_reporte, startRow = 1, startCol = 1)
addStyle(wb, ws, createStyle(fontName = "Arial", fontSize = 14,
                              textDecoration = "BOLD", fontColour = COL_HEADER),
         rows = 1, cols = 1)

```

### Tabla Resumen familia
```{r}
writeData(wb, ws, tabla_venta_subfamilia_JOYERIA,
          startRow = 3, startCol = 1, borders = "all")

total_row_sf  <- which(tabla_venta_subfamilia_JOYERIA$FAMILIA == "TOTAL")  + 3
#descto_row_sf <- which(tabla_venta_subfamilia_JOYERIA$SUBFAMILIA == "DESCUENTO") + 3
pct_col_sf    <- which(names(tabla_venta_subfamilia_JOYERIA) == "Participación")
num_cols_sf   <- which(names(tabla_venta_subfamilia_JOYERIA) %in%
                         c("Unidades", "Facturación"))

apply_table_styles(wb, ws, tabla_venta_subfamilia_JOYERIA,
                   start_row = 3, start_col = 1,
                   total_rows  = total_row_sf,
                  # descto_rows = descto_row_sf,
                   pct_cols  = pct_col_sf,
                   num_cols  = num_cols_sf)

setColWidths(wb, ws, cols = 1, widths = 28)
setColWidths(wb, ws, cols = 2:4, widths = 15)

# Tamaño de fuente 12 y altura de fila 21 para toda la tabla
st_size12 <- createStyle(fontSize = 12)
addStyle(wb, ws, st_size12,
         rows = 3:(3 + nrow(tabla_venta_subfamilia_JOYERIA)),
         cols = 1:ncol(tabla_venta_subfamilia_JOYERIA),
         gridExpand = TRUE, stack = TRUE)

setRowHeights(wb, ws,
              rows = 3:(3 + nrow(tabla_venta_subfamilia_JOYERIA)),
              heights = 21)

```

### Gráfico de barras horizontales por participación
```{r}

datos_chart_sf <- tabla_venta_subfamilia_JOYERIA %>%
  filter(!FAMILIA %in% c("TOTAL", "DESCUENTO")) %>%
  arrange(Participación) %>%
  mutate(FAMILIA = factor(FAMILIA, levels = FAMILIA))

p_sf <- ggplot(datos_chart_sf, aes(x = Participación, y = FAMILIA)) +
  geom_col(fill = COL_HEADER, width = 0.65) +
  geom_text(aes(label = percent(Participación, accuracy = 0.1)),
            hjust = -0.1, size = 3.2, colour = COL_HEADER) +
  scale_x_continuous(labels = percent,
                     expand = expansion(mult = c(0, 0.18))) +
  labs(title = "Participación por familia") +
  tema_reporte()

img_h_sf <- max(3, nrow(datos_chart_sf) * 0.38)
img_sf   <- save_plot_tmp(p_sf, width = 6.5, height = img_h_sf)
insertImage(wb, ws, img_sf, startRow = 3, startCol = 6,
            width = 6.5, height = img_h_sf, units = "in")

#freezePane(wb, ws, firstRow = FALSE, firstActiveRow = 4)

# Nota debajo de la tabla
fila_nota <- 3 + nrow(tabla_venta_subfamilia_JOYERIA) + 2
writeData(wb, ws,
          "* La diferencia en el total de facturación se debe a descuentos aplicados.",
          startRow = fila_nota, startCol = 1)
addStyle(wb, ws,
         createStyle(fontName = "Arial", fontSize = 9, fontColour = "#6C757D", textDecoration = "ITALIC"),
         rows = fila_nota, cols = 1)
```


# 2. SKU
```{r}

addWorksheet(wb, "SKU", gridLines = FALSE)
ws <- "SKU"

# Reordenar: TOTAL queda arriba, resto ordenado por PROVEEDOR asc / TOTAL desc
SKU_excel <- bind_rows(
  SKU_total_JOYERIA %>% filter(PROVEEDOR == "TOTAL"),
  SKU_total_JOYERIA %>% filter(PROVEEDOR != "TOTAL") %>% arrange(PROVEEDOR, desc(TOTAL))
) %>%
  mutate(IMAGEN = "") %>%
  relocate(IMAGEN, .before = PROVEEDOR)

writeData(wb, ws, SKU_excel, startRow = 1, startCol = 1, borders = "all")

total_row_sku <- which(SKU_excel$PROVEEDOR == "TOTAL") + 1
num_cols_sku  <- which(names(SKU_excel) %in%
                         c("VENTA", "PRECIO", "TOTAL", "STOCK", "D/S"))

apply_table_styles(wb, ws, SKU_excel,
                   start_row = 1, start_col = 1,
                   total_rows = total_row_sku,
                   num_cols   = num_cols_sku)

# Anchos: IMAGEN | PROVEEDOR | FAMILIA | CODIGO | PRODUCTO | ...
setColWidths(wb, ws, cols = 1, widths = 11)   # IMAGEN
setColWidths(wb, ws, cols = 2, widths = 58)   # PROVEEDOR
setColWidths(wb, ws, cols = 3, widths = 21)   # FAMILIA
setColWidths(wb, ws, cols = 4, widths = 14)   # CODIGO
setColWidths(wb, ws, cols = 5, widths = 90)   # PRODUCTO
setColWidths(wb, ws, cols = 6:ncol(SKU_excel), widths = 13)

# ---- Columna de imágenes (col 1) --------------------------------
img_folder <- file.path("imagenes")
img_files  <- list.files(img_folder, full.names = TRUE,
                          pattern = "\\.(jpg|jpeg|png|webp)$",
                          ignore.case = TRUE)
img_lookup <- tibble(
  filepath = img_files,
  ref      = str_extract(basename(img_files), "^\\d+")
) %>% filter(!is.na(ref))

IMG_IN <- 0.65
IMG_PT <- 50

filas_datos <- which(!SKU_excel$PROVEEDOR %in% c("TOTAL", "DESCUENTO"))

for (i in filas_datos) {
  excel_row <- i + 1
  setRowHeights(wb, ws, rows = excel_row, heights = IMG_PT)

  codigo <- as.character(SKU_excel$CODIGO[i])
  if (is.na(codigo) || codigo == "") next

  match_idx <- which(img_lookup$ref == codigo)
  if (length(match_idx) == 0 && nchar(codigo) >= 6)
    match_idx <- which(img_lookup$ref == substr(codigo, 1, 6))
  if (length(match_idx) == 0) next

  img_tmp <- compress_img(img_lookup$filepath[match_idx[1]])
  insertImage(wb, ws, file = img_tmp,
              startRow = excel_row, startCol = 1,
              width = IMG_IN, height = IMG_IN, units = "in")
}

freezePane(wb, ws, firstActiveRow = 2)
addFilter(wb, ws, row = 1, cols = 1:ncol(SKU_excel))

```

# 3. FAMILIAS
```{r}

addWorksheet(wb, "Familias", gridLines = FALSE)
ws <- "Familias"

# Agregar columna IMAGEN al principio
SKU_SUBFAMILIA_excel <- SKU_SUBFAMILIA_JOYERIA_final %>%
  mutate(IMAGEN = "") %>%
  relocate(IMAGEN, .before = PROVEEDOR)

writeData(wb, ws, SKU_SUBFAMILIA_excel, startRow = 1, startCol = 1, borders = "all")

total_rows_fam <- which(str_detect(
  replace_na(SKU_SUBFAMILIA_excel$PRODUCTO, ""), "^Total")) + 1
num_cols_fam   <- which(names(SKU_SUBFAMILIA_excel) %in%
                          c("VENTA", "PRECIO", "TOTAL", "STOCK", "D/S"))

apply_table_styles(wb, ws, SKU_SUBFAMILIA_excel,
                   start_row = 1, start_col = 1,
                   total_rows = total_rows_fam,
                   num_cols   = num_cols_fam)

# Anchos: IMAGEN | PROVEEDOR | FAMILIA | CODIGO | PRODUCTO | ...
setColWidths(wb, ws, cols = 1, widths = 11)
setColWidths(wb, ws, cols = 2, widths = 58)
setColWidths(wb, ws, cols = 3, widths = 22)
setColWidths(wb, ws, cols = 4, widths = 13)
setColWidths(wb, ws, cols = 5, widths = 86)
setColWidths(wb, ws, cols = 6:ncol(SKU_SUBFAMILIA_excel), widths = 13)

freezePane(wb, ws, firstActiveRow = 2)
addFilter(wb, ws, row = 1, cols = 1:ncol(SKU_SUBFAMILIA_excel))

# ---- Imágenes ----
filas_datos_fam <- which(!str_detect(
  replace_na(SKU_SUBFAMILIA_excel$PRODUCTO, ""), "^Total"))

for (i in filas_datos_fam) {
  excel_row <- i + 1
  setRowHeights(wb, ws, rows = excel_row, heights = IMG_PT)

  codigo <- as.character(SKU_SUBFAMILIA_excel$CODIGO[i])
  if (is.na(codigo) || codigo == "") next

  match_idx <- which(img_lookup$ref == codigo)
  if (length(match_idx) == 0 && nchar(codigo) >= 6)
    match_idx <- which(img_lookup$ref == substr(codigo, 1, 6))
  if (length(match_idx) == 0) next

  img_tmp <- compress_img(img_lookup$filepath[match_idx[1]])
  insertImage(wb, ws, file = img_tmp,
              startRow = excel_row, startCol = 1,
              width = IMG_IN, height = IMG_IN, units = "in")
}

```


# 4. MODELOS
```{r}

addWorksheet(wb, "Modelos", gridLines = FALSE)
ws <- "Modelos"

# Reordenar: TOTAL arriba; resto por PROVEEDOR asc, FAMILIA asc, TOTAL desc
Modelos_excel <- bind_rows(
  Modelos_total_JOYERIA %>% filter(PROVEEDOR == "TOTAL"),
  Modelos_total_JOYERIA %>% filter(PROVEEDOR != "TOTAL") %>%
    arrange(PROVEEDOR, FAMILIA, desc(TOTAL))
) %>%
  mutate(IMAGEN = "") %>%
  relocate(IMAGEN, .before = PROVEEDOR)

writeData(wb, ws, Modelos_excel, startRow = 1, startCol = 1, borders = "all")

total_row_mod <- which(Modelos_excel$PROVEEDOR == "TOTAL") + 1
num_cols_mod  <- which(names(Modelos_excel) %in%
                          c("VENTA", "PRECIO", "TOTAL", "STOCK", "D/S"))

apply_table_styles(wb, ws, Modelos_excel,
                   start_row = 1, start_col = 1,
                   total_rows = total_row_mod,
                   num_cols   = num_cols_mod)

# Anchos: IMAGEN | PROVEEDOR | FAMILIA | DESCRIPCION | CODIGO | ...
setColWidths(wb, ws, cols = 1, widths = 11)
setColWidths(wb, ws, cols = 2, widths = 58)
setColWidths(wb, ws, cols = 3, widths = 22)
setColWidths(wb, ws, cols = 4, widths = 63)
setColWidths(wb, ws, cols = 5:ncol(Modelos_excel), widths = 13)

freezePane(wb, ws, firstActiveRow = 2)
addFilter(wb, ws, row = 1, cols = 1:ncol(Modelos_excel))

# ---- Resaltar filas que representan el 80% de las ventas ----
total_fact_mod <- sum(
  Modelos_excel$TOTAL[!Modelos_excel$PROVEEDOR %in% c("TOTAL", "DESCUENTO")],
  na.rm = TRUE
)

modelos_top80 <- Modelos_excel %>%
  mutate(orig_row = row_number()) %>%
  filter(!PROVEEDOR %in% c("TOTAL", "DESCUENTO")) %>%
  arrange(desc(TOTAL)) %>%
  mutate(
    cum_pct  = cumsum(TOTAL) / total_fact_mod,
    cum_prev = lag(cum_pct, default = 0),
    es_80    = cum_prev < 0.80
  ) %>%
  filter(es_80) %>%
  pull(orig_row)

style_80 <- createStyle(fgFill = "#FFE699")  # amarillo claro
for (r in modelos_top80) {
  excel_row <- r + 1   # +1 por el encabezado
  addStyle(wb, ws, style_80,
           rows = excel_row, cols = 1:ncol(Modelos_excel),
           gridExpand = TRUE, stack = TRUE)
}

# ---- Imágenes ----
filas_datos_mod <- which(!Modelos_excel$PROVEEDOR %in% c("TOTAL", "DESCUENTO"))

for (i in filas_datos_mod) {
  excel_row <- i + 1
  setRowHeights(wb, ws, rows = excel_row, heights = IMG_PT)

  codigo <- as.character(Modelos_excel$CODIGO[i])
  if (is.na(codigo) || codigo == "") next

  match_idx <- which(img_lookup$ref == codigo)
  if (length(match_idx) == 0 && nchar(codigo) >= 6)
    match_idx <- which(img_lookup$ref == substr(codigo, 1, 6))
  if (length(match_idx) == 0) next

  img_tmp <- compress_img(img_lookup$filepath[match_idx[1]])
  insertImage(wb, ws, file = img_tmp,
              startRow = excel_row, startCol = 1,
              width = IMG_IN, height = IMG_IN, units = "in")
}

```


# 5. ATRIBUTOS
```{r}
addWorksheet(wb, "Atributos", gridLines = FALSE)
ws <- "Atributos"

# ---- Función: escribe título + tabla + mini-gráfico en un bloque ----
write_attr_block <- function(wb, ws, df, titulo,
                             start_row, start_col,
                             y_var,
                             x_var = "Participación") {

  # Ordenar siempre por Participación descendente
  df <- df %>% arrange(desc(Participación))

  n_cols_df <- ncol(df)
  n_rows_df <- nrow(df)

  # Identificar filas que representan el 80% acumulado
  df_marc <- df %>%
    mutate(cum_part = cumsum(Participación),
           cum_prev = lag(cum_part, default = 0),
           es_80 = cum_prev < 0.80 & (cum_prev + cum_part) / 2 < 0.80)
  filas_80 <- which(df_marc$es_80)

  # Título del bloque
  writeData(wb, ws, titulo, startRow = start_row, startCol = start_col)
  addStyle(wb, ws, st_titulo_bloque, rows = start_row, cols = start_col)

  # Tabla
  writeData(wb, ws, df, startRow = start_row + 1,
            startCol = start_col, borders = "all")

  pct_idx <- which(names(df) %in% c("Participación", "Acumulado"))
  num_idx <- which(names(df) %in% c("Facturación", "Cantidad"))

  apply_table_styles(wb, ws, df,
                     start_row = start_row + 1, start_col = start_col,
                     pct_cols = if (length(pct_idx) > 0) pct_idx else NULL,
                     num_cols = if (length(num_idx) > 0) num_idx else NULL)

  # Resaltar filas dentro del 80%
  style_80_attr <- createStyle(fgFill = "#FFE699")
  for (r in filas_80) {
    excel_row <- start_row + 1 + r
    addStyle(wb, ws, style_80_attr,
             rows = excel_row,
             cols = start_col:(start_col + n_cols_df - 1),
             gridExpand = TRUE, stack = TRUE)
  }

  # Anchos de columna
  setColWidths(wb, ws, cols = start_col, widths = 22)
  if (n_cols_df > 1)
    setColWidths(wb, ws, cols = (start_col + 1):(start_col + n_cols_df - 1),
                 widths = 13)

  # Gráfico: por Cantidad descendente
  df_plot <- df %>%
    arrange(Cantidad) %>%   # asc para que el mayor quede arriba en el eje Y
    mutate(etiq = factor(.data[[y_var]], levels = .data[[y_var]]))

  p <- ggplot(df_plot, aes(x = Cantidad, y = etiq)) +
    geom_col(fill = COL_HEADER, width = 0.65) +
    geom_text(aes(label = format(Cantidad, big.mark = ".")),
              hjust = -0.1, size = 2.8, colour = COL_HEADER) +
    scale_x_continuous(
      labels = scales::label_number(accuracy = 1),   # ← enteros
      expand = expansion(mult = c(0, 0.15))
    ) +
    labs(title = titulo, x = "Cantidad") +            # ← etiqueta eje X
    tema_reporte() +
    theme(axis.title.x = element_text(size = 9, colour = "grey40"))  # ← mostrar eje X

  img_h <- max(2, n_rows_df * 0.32)
  tmp   <- save_plot_tmp(p, width = 4.5, height = img_h)
  img_col <- start_col + n_cols_df + 1
  insertImage(wb, ws, tmp, startRow = start_row + 1, startCol = img_col,
              width = 4.5, height = img_h, units = "in")

  max(n_rows_df + 3, ceiling(img_h / 0.21) + 2)
}

# ---- Layout de la hoja Atributos ----------------------------
COL_A <- 1    # Cadenas  / Zodiaco   / Dije
COL_B <- 12   # Pulseras / Dos Letras
COL_C <- 23   # Anillos  / Inicial

cur_row <- 1

# ══════════════════════════════════════
# SECCIÓN 1: TALLES
# ══════════════════════════════════════
writeData(wb, ws, "── TALLES ──", startRow = cur_row, startCol = 1)
addStyle(wb, ws, st_titulo_sec, rows = cur_row, cols = 1)
cur_row <- cur_row + 2

h_ctalle <- write_attr_block(wb, ws, CADENAS_TALLE,  "Cadenas",  cur_row, COL_A,
                              y_var = "TALLE")
h_ptalle <- write_attr_block(wb, ws, PULSERAS_TALLE, "Pulseras", cur_row, COL_B,
                              y_var = "TALLE")
h_atalle <- write_attr_block(wb, ws, ANILLOS_TALLE,  "Anillos",  cur_row, COL_C,
                              y_var = "TALLE")

cur_row <- cur_row + max(h_ctalle, h_ptalle, h_atalle) + 2

# ══════════════════════════════════════
# SECCIÓN 2: GROSOR
# ══════════════════════════════════════
writeData(wb, ws, "── GROSOR ──", startRow = cur_row, startCol = 1)
addStyle(wb, ws, st_titulo_sec, rows = cur_row, cols = 1)
cur_row <- cur_row + 2

h_cgros <- write_attr_block(wb, ws, CADENAS_GROSOR,  "Cadenas",  cur_row, COL_A,
                             y_var = "GROSOR")
h_pgros <- write_attr_block(wb, ws, PULSERAS_GROSOR, "Pulseras", cur_row, COL_B,
                             y_var = "GROSOR")

cur_row <- cur_row + max(h_cgros, h_pgros) + 2

# ══════════════════════════════════════
# SECCIÓN 3: VARIANTES (Anillos)
# ══════════════════════════════════════
writeData(wb, ws, "── VARIANTES ──", startRow = cur_row, startCol = 1)
addStyle(wb, ws, st_titulo_sec, rows = cur_row, cols = 1)
cur_row <- cur_row + 2

h_zodiac <- write_attr_block(wb, ws, ANILLOS_ZODIACO_VARIANTE,    "Zodiaco",    cur_row, COL_A,
                              y_var = "VARIANTE")
h_doslet <- write_attr_block(wb, ws, ANILLOS_DOS_LETRAS_VARIANTE, "Dos Letras", cur_row, COL_B,
                              y_var = "VARIANTE")
h_inic   <- write_attr_block(wb, ws, ANILLOS_INICIAL_VARIANTE,    "Inicial",    cur_row, COL_C,
                              y_var = "VARIANTE")

cur_row <- cur_row + max(h_zodiac, h_doslet, h_inic) + 2

# ══════════════════════════════════════
# SECCIÓN 4: DIJES
# ══════════════════════════════════════
writeData(wb, ws, "── DIJES ──", startRow = cur_row, startCol = 1)
addStyle(wb, ws, st_titulo_sec, rows = cur_row, cols = 1)
cur_row <- cur_row + 2

write_attr_block(wb, ws, DIJE_INICIAL_VARIANTE, "Dije Inicial – Variante", cur_row, COL_A,
                 y_var = "VARIANTE")

```


# 6. PROVEEDORES
```{r}

ws <- "Proveedores"
addWorksheet(wb, ws, gridLines = FALSE)

# Tabla resumen general (arriba a la derecha)
writeData(wb, ws, "Resumen por proveedor", startRow = 1, startCol = 6)
addStyle(wb, ws, st_titulo_bloque, rows = 1, cols = 6)
writeData(wb, ws, PROVEEDOR_JOYERIA_TOTAL, startRow = 2, startCol = 6, borders = "all")

total_row_pg <- which(PROVEEDOR_JOYERIA_TOTAL$PROVEEDOR == "TOTAL") + 2
pct_col_pg   <- which(names(PROVEEDOR_JOYERIA_TOTAL) == "Participación")
num_cols_pg  <- which(names(PROVEEDOR_JOYERIA_TOTAL) %in% c("Unidades", "Facturación"))

apply_table_styles(wb, ws, PROVEEDOR_JOYERIA_TOTAL,
                   start_row = 2, start_col = 6,
                   total_rows = total_row_pg,
                   pct_cols   = pct_col_pg,
                   num_cols   = num_cols_pg)

setColWidths(wb, ws, cols = 6,   widths = 35)
setColWidths(wb, ws, cols = 7:8, widths = 14)
setColWidths(wb, ws, cols = 9,   widths = 12)

# ---- Preparar datos para el gráfico ----
datos_prov_chart <- PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA %>%
  filter(FAMILIA != "TOTAL") %>%
  filter(!str_detect(PROVEEDOR, "^Total")) %>%
  ungroup()

# Orden de proveedores por facturación total descendente (mayor a la izquierda en cada barra)
prov_order <- datos_prov_chart %>%
  group_by(PROVEEDOR) %>%
  summarise(tot = sum(Facturación, na.rm = TRUE)) %>%
  arrange(desc(tot)) %>%
  pull(PROVEEDOR)


# Orden de proveedores dentro de cada barra: mayor facturación total primero
prov_order <- datos_prov_chart %>%
  group_by(PROVEEDOR) %>%
  summarise(tot = sum(Facturación, na.rm = TRUE)) %>%
  arrange(desc(tot)) %>%
  pull(PROVEEDOR)

datos_prov_chart2 <- datos_prov_chart %>%
  mutate(
    PROVEEDOR = str_trunc(PROVEEDOR, width = 28, ellipsis = "…"),
    PROVEEDOR = factor(PROVEEDOR,
                       levels = str_trunc(prov_order, width = 28, ellipsis = "…"))
  )

n_familias <- n_distinct(datos_prov_chart2$FAMILIA)
# Paleta dinámica: soporta cualquier cantidad de proveedores
n_provs  <- n_distinct(datos_prov_chart2$PROVEEDOR)
colores  <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_provs)

p_prov <- ggplot(datos_prov_chart2,
                 aes(x = Participación, y = FAMILIA, fill = PROVEEDOR)) +
  geom_col(position = "stack", width = 0.65) +
  scale_x_continuous(labels = percent,
                     expand = expansion(mult = c(0, 0.02))) +
  scale_fill_manual(values = colores, name = "Proveedor") +
  labs(title = "Participación por Proveedor dentro de cada familia") +
  tema_reporte() +
  theme(
    legend.position = "right",
    legend.text     = element_text(size = 8),
    legend.key.size = unit(0.4, "cm"),
    legend.title    = element_text(size = 9, face = "bold")
  )

img_h_prov <- max(4, n_familias * 0.55)
img_prov   <- save_plot_tmp(p_prov, width = 11, height = img_h_prov)
insertImage(wb, ws, img_prov, startRow = 1, startCol = 1,
            width = 11, height = img_h_prov, units = "in")

# ---- Tabla detalle debajo del gráfico ----
data_start_prov <- ceiling(img_h_prov / 0.21) + 3

writeData(wb, ws, "Ventas por familia y proveedor",
          startRow = data_start_prov - 1, startCol = 1)
addStyle(wb, ws, st_titulo_bloque, rows = data_start_prov - 1, cols = 1)
writeData(wb, ws, PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA,
          startRow = data_start_prov, startCol = 1, borders = "all")

total_rows_ps <- which(PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA$FAMILIA == "TOTAL") +
  data_start_prov
pct_col_ps    <- which(names(PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA) == "Participación")
num_cols_ps   <- which(names(PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA) %in%
                         c("Unidades", "Facturación"))

subt_rows_ps <- which(
  str_detect(PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA$PROVEEDOR, "^Total ") &
  PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA$PROVEEDOR != "TOTAL"
) + data_start_prov

for (r in subt_rows_ps)
  addStyle(wb, ws, st_total, rows = r,
           cols = 1:ncol(PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA),
           gridExpand = TRUE, stack = FALSE)

apply_table_styles(wb, ws, PROVEEDOR_SUBFAMILIAS_TOTAL_JOYERIA,
                   start_row = data_start_prov, start_col = 1,
                   total_rows = total_rows_ps,
                   pct_cols   = pct_col_ps,
                   num_cols   = num_cols_ps)

setColWidths(wb, ws, cols = 1:2, widths = 35)
setColWidths(wb, ws, cols = 3:4, widths = 14)
setColWidths(wb, ws, cols = 5,   widths = 58)

```


# 7. NACIONALIDAD
```{r}

addWorksheet(wb, "Nacionalidad", gridLines = FALSE)
ws <- "Nacionalidad"

# Los nombres de columna después del left_join quedan con sufijos .x / .y
# .x = cantidades  |  .y = facturación
col_nac   <- names(NACIONALIDAD_ENTERA_JOYERIA)
has_sufij <- any(str_detect(col_nac, "\\.x$|\\.y$"))

if (has_sufij) {
  # Renombrar para claridad en encabezados del Excel
  NACIONLIDAD_EXCEL <- NACIONALIDAD_ENTERA_JOYERIA %>%
    rename_with(~ str_replace(.x, "\\.x$", " (cant.)"),
                ends_with(".x")) %>%
    rename_with(~ str_replace(.x, "\\.y$", " (fact.)"),
                ends_with(".y"))
} else {
  NACIONLIDAD_EXCEL <- NACIONALIDAD_ENTERA_JOYERIA
}

col_fact_g2 <- if (has_sufij) "Güemes 2.y" else "Güemes 2"
col_fact_es <- if (has_sufij) "El Solar.y"  else "El Solar"
col_fact_st2 <- if (has_sufij) "San Telmo 2.y" else "San Telmo 2"

datos_nac_base <- NACIONALIDAD_ENTERA_JOYERIA %>%
  filter(NACIONALIDAD != "Total general") %>%
  select(NACIONALIDAD,
         G2  = all_of(col_fact_g2),
         ES  = all_of(col_fact_es),
         ST2 = all_of(col_fact_st2)) %>%
  mutate(total_nac = G2 + ES + ST2)

total_g2  <- sum(datos_nac_base$G2,  na.rm = TRUE)
total_es  <- sum(datos_nac_base$ES,  na.rm = TRUE)
total_st2 <- sum(datos_nac_base$ST2, na.rm = TRUE)

orden_nac <- datos_nac_base %>%
  arrange(total_nac) %>%
  pull(NACIONALIDAD)

# Gráfico: participación de cada nacionalidad dentro de la facturación de su tienda
datos_nac_chart <- datos_nac_base %>%
  mutate(
    `Güemes 2`    = G2  / total_g2,
    `El Solar`    = ES  / total_es,
    `San Telmo 2` = ST2 / total_st2,
    NACIONALIDAD  = factor(NACIONALIDAD, levels = orden_nac)
  ) %>%
  select(NACIONALIDAD, `Güemes 2`, `El Solar`, `San Telmo 2`) %>%
  pivot_longer(cols = c("Güemes 2", "El Solar", "San Telmo 2"),
               names_to = "Tienda", values_to = "Participación") %>%
  filter(Participación > 0)

p_nac <- ggplot(datos_nac_chart,
                aes(x = Participación, y = NACIONALIDAD, fill = Tienda)) +
  geom_col(width = 0.65) +
  geom_text(aes(label = percent(Participación, accuracy = 0.1)),
            hjust = -0.1, size = 2.8, colour = "grey30") +
  scale_x_continuous(labels = percent,
                     expand = expansion(mult = c(0, 0.2))) +
  scale_fill_manual(values = c("Güemes 2"    = COL_HEADER,
                                "El Solar"    = "#5B9BD5",
                                "San Telmo 2" = "#70AD47")) +
  facet_wrap(~ Tienda, nrow = 1, scales = "free_y") +
  labs(title = "Participación de cada Nacionalidad en la facturación por Tienda") +
  tema_reporte() +
  theme(legend.position = "none",
        strip.text = element_text(face = "bold", colour = COL_HEADER, size = 10))

n_nac     <- max(
  sum(datos_nac_chart$Tienda == "Güemes 2"),
  sum(datos_nac_chart$Tienda == "El Solar"),
  sum(datos_nac_chart$Tienda == "San Telmo 2")
)

img_h_nac <- max(3.5, n_nac * 0.4)
img_nac   <- save_plot_tmp(p_nac, width = 14, height = img_h_nac)
insertImage(wb, ws, img_nac, startRow = 1, startCol = 1,
            width = 14, height = img_h_nac, units = "in")

data_start_nac <- ceiling(img_h_nac / 0.21) + 3
writeData(wb, ws, NACIONLIDAD_EXCEL,
          startRow = data_start_nac, startCol = 1, borders = "all")

total_rows_nac <- which(NACIONLIDAD_EXCEL$NACIONALIDAD == "Total general") +
  data_start_nac
num_cols_nac   <- 2:ncol(NACIONLIDAD_EXCEL)

apply_table_styles(wb, ws, NACIONLIDAD_EXCEL,
                   start_row = data_start_nac, start_col = 1,
                   total_rows = total_rows_nac,
                   num_cols   = num_cols_nac)

setColWidths(wb, ws, cols = 1, widths = 22)
setColWidths(wb, ws, cols = 2:ncol(NACIONLIDAD_EXCEL), widths = 18)

```


# 8. MÉTODO DE PAGO
```{r}

addWorksheet(wb, "Método de pago", gridLines = FALSE)
ws <- "Método de pago"

# Tabla
writeData(wb, ws, Metodo_pago_Odoo_resumido,
          startRow = 1, startCol = 1, borders = "all")

pct_col_mp  <- which(names(Metodo_pago_Odoo_resumido) == "PARTICIPACION")
num_cols_mp <- which(names(Metodo_pago_Odoo_resumido) %in% c("FACTURACION", "VENTA"))

apply_table_styles(wb, ws, Metodo_pago_Odoo_resumido,
                   start_row = 1, start_col = 1,
                   pct_cols = pct_col_mp,
                   num_cols = num_cols_mp)

setColWidths(wb, ws, cols = 1, widths = 15)
setColWidths(wb, ws, cols = 2, widths = 20)
setColWidths(wb, ws, cols = 3:5, widths = 15)

# ---- Función donut ------------------------------------
donut_plot <- function(datos, tienda) {
  df <- datos %>%
    filter(TIENDA == tienda) %>%
    arrange(desc(PARTICIPACION)) %>%
    mutate(CONCEPTO = factor(CONCEPTO, levels = CONCEPTO))

  ggplot(df, aes(x = 2, y = PARTICIPACION, fill = CONCEPTO)) +
    geom_col(width = 1, colour = "white", linewidth = 0.5) +
    geom_text(
      aes(label = ifelse(PARTICIPACION >= 0.04,
                   gsub("\\.", ",", sprintf("%.1f%%", PARTICIPACION * 100)), "")),
      position = position_stack(vjust = 0.5),
      size = 3.2, colour = "grey20", fontface = "bold"
    ) +
    coord_polar(theta = "y", start = 0) +
    xlim(0.5, 2.5) +
    scale_fill_brewer(palette = "Set2", name = NULL) +
    labs(title = tienda) +
    theme_void() +
    theme(
      plot.title      = element_text(colour = COL_HEADER, face = "bold",
                                     size = 11, hjust = 0.5),
      legend.position = "bottom",
      legend.text     = element_text(size = 8),
      legend.key.size = unit(0.35, "cm")
    )
}

# ---- Generar y posicionar los dos donuts ----------------
img_h_donut <- 3.8
img_w_donut <- 5.5
col_solar   <- ncol(Metodo_pago_Odoo_resumido) + 2
col_guemes  <- col_solar + 7
col_st2     <- col_guemes + 7

writeData(wb, ws, "Distribución de los métodos de pago utilizados, por tienda",
          startRow = 1, startCol = col_solar+4)
addStyle(wb, ws, st_titulo_bloque, rows = 1, cols = col_solar+4)

insertImage(wb, ws,
            save_plot_tmp(donut_plot(Metodo_pago_Odoo_resumido, "El Solar"),
                          width = img_w_donut, height = img_h_donut),
            startRow = 2, startCol = col_solar,
            width = img_w_donut, height = img_h_donut, units = "in")

insertImage(wb, ws,
            save_plot_tmp(donut_plot(Metodo_pago_Odoo_resumido, "Güemes 2"),
                          width = img_w_donut, height = img_h_donut),
            startRow = 2, startCol = col_guemes,
            width = img_w_donut, height = img_h_donut, units = "in")

insertImage(wb, ws,
            save_plot_tmp(donut_plot(Metodo_pago_Odoo_resumido, "San Telmo 2"),
                          width = img_w_donut, height = img_h_donut),
            startRow = 2, startCol = col_st2,
            width = img_w_donut, height = img_h_donut, units = "in")

```


# 9. PRECIO UNITARIO
```{r}

addWorksheet(wb, "Precio unitario", gridLines = FALSE)
ws <- "Precio unitario"

# -- Tabla general (izquierda) --
writeData(wb, ws, "Por precio", startRow = 1, startCol = 1)
addStyle(wb, ws, st_titulo_bloque, rows = 1, cols = 1)
writeData(wb, ws, PRECIO_UNITARIO_JOYERIA,
          startRow = 2, startCol = 1, borders = "all")

pct_col_pu  <- which(names(PRECIO_UNITARIO_JOYERIA) %in% c("Participación", "Acumulado"))
num_cols_pu <- which(names(PRECIO_UNITARIO_JOYERIA) %in% c("Precio", "Cantidad", "Facturación"))

apply_table_styles(wb, ws, PRECIO_UNITARIO_JOYERIA,
                   start_row = 2, start_col = 1,
                   pct_cols = pct_col_pu, num_cols = num_cols_pu)

n_cols_pu <- ncol(PRECIO_UNITARIO_JOYERIA)
setColWidths(wb, ws, cols = 1:n_cols_pu, widths = 14)

# Gráfico tabla general (debajo)
p_pu <- ggplot(PRECIO_UNITARIO_JOYERIA,
               aes(x = reorder(as.character(Precio), -Facturación))) +
  geom_col(aes(y = Facturación), fill = COL_HEADER, width = 0.7) +
  scale_y_continuous(labels = comma) +
  scale_x_discrete(labels = function(x)
    format(as.numeric(x), big.mark = ".", scientific = FALSE)) +
  labs(title = "Distribución de ventas por precio unitario", x = "Precio") +
  tema_reporte() +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))

img_pu <- save_plot_tmp(p_pu, width = 7, height = 4)
insertImage(wb, ws, img_pu,
            startRow = nrow(PRECIO_UNITARIO_JOYERIA) + 4, startCol = 1,
            width = 7, height = 4, units = "in")

# -- Tabla por familia (a la derecha) --
col_sf_pu <- n_cols_pu + 3
writeData(wb, ws, "Por familia y precio", startRow = 1, startCol = col_sf_pu)
addStyle(wb, ws, st_titulo_bloque, rows = 1, cols = col_sf_pu)
writeData(wb, ws, PRECIO_UNITARIO_SUBFAMILIA_JOYERIA,
          startRow = 2, startCol = col_sf_pu, borders = "all")

pct_col_psf  <- which(names(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA) %in% c("Participación", "Acumulado"))
num_cols_psf <- which(names(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA) %in% c("Precio", "Cantidad", "Facturación"))

apply_table_styles(wb, ws, PRECIO_UNITARIO_SUBFAMILIA_JOYERIA,
                   start_row = 2, start_col = col_sf_pu,
                   pct_cols = pct_col_psf, num_cols = num_cols_psf)

# Resaltar filas de subtotal por familia
subt_rows_psf <- which(str_detect(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA$FAMILIA, "^Total ")) + 2
for (r in subt_rows_psf) {
  addStyle(wb, ws, st_total,
           rows = r,
           cols = col_sf_pu:(col_sf_pu + ncol(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA) - 1),
           gridExpand = TRUE, stack = FALSE)
  addStyle(wb, ws, st_pct,
           rows = r, cols = col_sf_pu + pct_col_psf - 1,
           gridExpand = TRUE, stack = TRUE)
  addStyle(wb, ws, st_number,
           rows = r, cols = col_sf_pu + num_cols_psf - 1,
           gridExpand = TRUE, stack = TRUE)
}

setColWidths(wb, ws,
             cols = col_sf_pu:(col_sf_pu + ncol(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA) - 1),
             widths = 14)
setColWidths(wb, ws, cols = col_sf_pu, widths = 22)

# -- Gráficos por familia (uno debajo del otro, a la derecha de la tabla) --
familias_ord <- unique(PRECIO_UNITARIO_SUBFAMILIA_datos$FAMILIA)
chart_col    <- col_sf_pu + ncol(PRECIO_UNITARIO_SUBFAMILIA_JOYERIA) + 2

img_h_chart <- 2.8
img_w_chart <- 5

cur_chart_row <- 1
for (fam in familias_ord) {
  datos_fam <- PRECIO_UNITARIO_SUBFAMILIA_datos %>%
    filter(FAMILIA == fam) %>%
    arrange(Precio)

  if (nrow(datos_fam) == 0) next

  p_fam <- ggplot(datos_fam, aes(x = factor(Precio), y = Cantidad)) +
    geom_col(fill = COL_HEADER, width = 0.7) +
    geom_text(aes(label = format(Cantidad, big.mark = ".")),
              vjust = -0.3, size = 2.6, colour = COL_HEADER) +
    scale_x_discrete(labels = function(x)
      format(as.numeric(x), big.mark = ".", scientific = FALSE)) +
    scale_y_continuous(expand = expansion(mult = c(0, 0.15))) +
    labs(title = fam, x = "Precio", y = "Cantidad") +
    tema_reporte() +
    theme(axis.text.x = element_text(angle = 45, hjust = 1, size = 8))

  tmp <- save_plot_tmp(p_fam, width = img_w_chart, height = img_h_chart)
  insertImage(wb, ws, tmp,
              startRow = cur_chart_row, startCol = chart_col,
              width = img_w_chart, height = img_h_chart, units = "in")

  cur_chart_row <- cur_chart_row + ceiling(img_h_chart / 0.21) + 1
}

```


# 10. VALOR STOCK
```{r}
addWorksheet(wb, "Valor stock", gridLines = FALSE)
ws <- "Valor stock"

writeData(wb, ws, STOCK_VALOR_TOTAL_JOYERIA,
          startRow = 1, startCol = 1, borders = "all")

total_rows_vs <- which(STOCK_VALOR_TOTAL_JOYERIA$FAMILIA == "Total") + 1
num_cols_vs   <- 2:ncol(STOCK_VALOR_TOTAL_JOYERIA)

apply_table_styles(wb, ws, STOCK_VALOR_TOTAL_JOYERIA,
                   start_row = 1, start_col = 1,
                   total_rows = total_rows_vs,
                   num_cols   = num_cols_vs)

# Escala de color blanco → azul oscuro por columna (excluye fila Total)
n_data_vs <- nrow(STOCK_VALOR_TOTAL_JOYERIA) - 1

setColWidths(wb, ws, cols = 1, widths = 25)
setColWidths(wb, ws, cols = 2:ncol(STOCK_VALOR_TOTAL_JOYERIA), widths = 18)

```



#Aviso en caso de faltantes de datos o duplicaciones
```{r}
# ---- Resumen de validaciones --------------------------------
revisar_todos <- list(
  SUBFAMILIA          = REVISAR_SUBFAMILIA_JOYERIA,
  PROVEEDORES         = REVISAR_PROVEEDORES_JOYERIA,
  STOCK_PRECIO        = REVISAR_STOCK_SIN_PRECIO_JOYERIA,
  PROV_DUPLICADOS     = REVISO_PROVEEDORES,
  PRECIO_DUPLICADOS   = REVISAR_MAS_DE_UN_PRECIO
)

con_datos <- Filter(function(df) nrow(df) > 0, revisar_todos)

if (length(con_datos) == 0) {
  message("✅  Todo OK — ningún REVISAR tiene filas.")
} else {
  message("════════════════════════════════════")
  message("⚠️  REVISAR con datos — corregir antes de cerrar:")
  for (nm in names(con_datos)) {
    message("  • ", nm, ": ", nrow(con_datos[[nm]]), " fila(s)")
  }
  message("════════════════════════════════════")
}
```


# GUARDAR
```{r}

nombre_archivo <- paste0("Reporte_ventas/Reporte_Ventas_JOYERIA_HISTORICO_", format(fecha_fin, "%Y%m%d"), ".xlsx")
saveWorkbook(wb, nombre_archivo, overwrite = TRUE)
message("\u2714 Guardado: ", nombre_archivo)

```

